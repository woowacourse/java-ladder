package laddergame.domain;

import org.junit.jupiter.api.Test;

import java.util.Arrays;

import static org.assertj.core.api.AssertionsForClassTypes.assertThat;
import static org.junit.jupiter.api.Assertions.assertThrows;
/*

public class PrizesGeneratorTest {
    @Test
    void 상품클래스가_제대로_만들어지는지_테스트() {
        String prizes = "꽝,꽝,1억, 100억";
        Prize testPrizes = new Prize(prizes);
        assertThat(testPrizes).isEqualTo(new Prize(prizes));
    }

    @Test
    void 제대로_prize리스트_반환하는지_테스트() {
        assertThat(PrizesGenerator.createPrizes("a,b,c", 3)).isEqualTo(Arrays.asList(new Prize("a"), new Prize("b"), new Prize("c")));
    }

    @Test
    void 플레이어수와_상품수가_다를때_테스트() {
        assertThrows(IllegalArgumentException.class, () ->
                PrizesGenerator.createPrizes("a,b,c", 7));
    }
}

 */
