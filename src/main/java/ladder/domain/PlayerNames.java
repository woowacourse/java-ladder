package ladder.domain;

import java.util.stream.Collectors;
import ladder.util.StringSplitter;

public class PlayerNames extends Names {

    public PlayerNames(String names) {
        super(names);
    }
}
